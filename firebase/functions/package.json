{
  "name": "aquacare-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for AquaCareSystem",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "jest"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "firebase-tools": "^13.0.0"
  },
  "engines": {
    "node": "18"
  }
}
